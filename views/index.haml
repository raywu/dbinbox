.col-xs-10.col-xs-offset-1.col-sm-8.col-sm-offset-2.col-md-6.col-md-offset-3.col-lg-6.col-lg-offset-3
  %h1 
    <a href="/"><img src="/img/domokun_logo.gif" style="height:2em" /></a>
    Domo Kun Photos!
    %small Collect photos from friends

  -if !session[:registered] # if the user isn't registered
    %form.form-inline{action: "/", method: "post"}
      %fieldset.control-group{class: @error.nil? || @error.empty? ? "" : "error"}
        .input-prepend
          %span.add-on DomoKunPhotos.com/
          %input#username(placeholder="create_username" name="username" type="text" value="#{params['username']}")
          %button.btn.btn-success(type='submit' title="links with Dropbox") link with Dropbox
        %span.help-inline=@error
  -else
    %p{style: "color: green"} You're registered!
    %p
      -url = url('/') + session[:username] if session[:username]
      %a{href: url}=url

.col-xs-10.col-xs-offset-1.col-sm-8.col-sm-offset-2.col-md-6.col-md-offset-3.col-lg-6.col-lg-offset-3
  %h2
    Instructions
  %p
    Select a new user name to set up an unique URL for your Domo Kun Photos collection.
  %p
    Domo Kun Photos will ask to authenticate with your Dropbox account.
  %h2
    About
  %p
    Domo Kun Photos is open source. This %a{href: "https://github.com/raywu/dbinbox"} project
    is originally forked from %a{href: "https://github.com/christiangenco/dbinbox"} christiangenco/dbinbox
    .